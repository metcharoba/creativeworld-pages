# CreativeWorld

CreativeWorld is a small static personal homepage experiment.

It is a quiet composition that keeps rearranging itself — a generative field you
enter, with small rooms, objects, and traces left inside the same space.

Open `index.html` directly in a browser to view it.
